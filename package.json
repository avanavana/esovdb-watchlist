{
  "name": "esovdb-watchlist-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone watchlist runner for ESOVDB (TypeScript + GitHub Actions).",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "dev": "npm run build && node --enable-source-maps dist/index.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.5.4"
  }
}
